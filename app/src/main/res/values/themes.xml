<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.HealthRecords" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="checkBox_dosage" parent="android:Widget.CompoundButton.CheckBox">
        <item name="android:button">@color/transparent</item>
    </style>

    <style name="radioButton_bad" parent="android:Widget.CompoundButton.RadioButton">
        <item name="android:button">@drawable/segmented_bad</item>
    </style>

    <style name="Theme.HealthRecords" parent="Base.Theme.HealthRecords" />
</resources>